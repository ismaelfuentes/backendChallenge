jest.mock('express-validator');
jest.mock('express');

describe('common utils', () => {
    describe('checkValidationErrors function', () => {
      // NOTE: won't invest more time in more unit testing, happy to do in real apps though :)
      test.todo('next function called when there are no errors');
      test.todo('response with 400 if errors found');
    });
});
